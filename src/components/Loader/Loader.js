import React from 'react';
import Header from '../Header/Header';

const Loader = () => (
  <React.Fragment>
    <Header nologo />
    <div className="loader" />
  </React.Fragment>
);

export default Loader;
